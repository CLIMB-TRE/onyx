name: onyx

services:
  wsgi-server:
    build: .
    ports:
      - 8000:8000
    volumes:
      - ${ONYX_PROJECTS_DIR}:/app/projects
    env_file:
      - ./onyx/.env
    # depends_on:
    #   db:
    #     condition: service_healthy

#   db:
#     image: postgres:14
#     restart: always
#     user: postgres
#     expose:
#       - 5432
#     volumes:
#       - pg-data:/var/lib/postgresql/data
#     # env_file:
#     #   - ./onyx/.env
#     environment:
#       - DATABASE_NAME=onyx
#       - DATABASE_USER=postgres
#       - DATABASE_PASSWORD=postgres
#       - DATABASE_HOST=localhost
#     healthcheck:
#       test: [ "CMD", "pg_isready" ]
#       interval: 10s
#       timeout: 5s
#       retries: 5

# volumes:
#   pg-data:
